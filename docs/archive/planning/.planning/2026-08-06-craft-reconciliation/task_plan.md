# Craft 与仓库进度对账计划

## 目标

读取 Craft 中的「Lyric Island｜总规划与想法（唯一页面）」，以当前仓库、测试和实际运行状态为准，修正其中的进度与主线，并产出一份不混淆 V3、Direction D/V4、历史规划和未来想法的执行路线图。

## 边界

- 本次只审计仓库并更新规划文档，不修改 V3/V4 产品代码。
- V3 作为当前可交付主线；Direction D/V4 作为独立实验线，除非仓库证据显示已完成，否则不并入当前完成度。
- 保留用户已有想法与历史记录；优先新增明确的「仓库对账」区块，不擅自删除原文。
- 不整理、覆盖或回滚当前脏工作区中的用户改动。

## 阶段

### Phase 1 — 读取 Craft 与建立文档地图（complete）

- [x] 发现并调用 Craft 连接器
- [x] 定位目标页面及其实际正文
- [x] 摘取当前结构、状态声明和待办

### Phase 2 — 审计仓库实际进度（complete）

- [x] 记录分支、HEAD、工作区状态与近期提交
- [x] 对照根目录和 `.planning/` 中的历史计划
- [x] 核对 V3、Direction D/V4、测试、构建与运行状态
- [x] 将可验证事实写入 `findings.md`

### Phase 3 — 对账与规划设计（complete）

- [x] 区分「已验证」「已实现待验收」「暂停」「待做」「想法池」
- [x] 确立单一当前主线和恢复实验线的门槛
- [x] 形成阶段化执行计划与近期三步
- [x] 向用户呈现 2–3 种 Craft 收口方式并取得写入结构确认

### Phase 4 — 更新 Craft（complete）

- [x] 在目标页写入 2026-08-06 仓库对账结果
- [x] 保留原始想法并标明历史/待确认内容
- [x] 回读更新后的页面验证内容

### Phase 5 — 交付总结（complete）

- [x] 汇总真实完成度、主要债务和当前决策
- [x] 以 Craft 为用户可见入口，不要求用户阅读本地执行镜像

## 决策记录

| 日期 | 决策 | 原因 |
|---|---|---|
| 2026-08-06 | 新建独立 scoped plan，不覆盖根目录 Phase 3.1/3.2 计划 | 旧计划与当前对账目标不同，直接复用会继续混淆主线 |
| 2026-08-06 | 推荐在原唯一页面置顶“当前执行看板”，不重写整页 | 最能保留历史证据，同时明确 supersede 关系并恢复单一当前主线 |

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 直接执行 `swift Tests/ruby_token_contract.swift` 找不到产品源码中的日语读音类型，且 `@main` 需要 library 解析 | 1 | 改用仓库正式入口 `Tests/ruby_layout_contract.sh`（组合编译产品源码与测试）；已 PASS。首次命令后未运行的 V3 contracts 也已单独全部复跑并通过。 |
| `direction_d_phase_3_3_contracts.sh` 的旧断言 `no_d_default_layout` 失败 | 1 | 对照 Phase 3.4 新合同确认：V4 现在合法拥有独立 `MainWindowLayoutStyle`，但 V3 仍是 default。记录为过期合同，不按旧语义回退当前代码。 |
